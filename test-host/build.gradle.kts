// Plugin versions for the standalone single-APK test host build.
// They intentionally match the main App-Collection project.
plugins {
    id("com.android.application") version "9.3.0" apply false
    id("com.android.library") version "9.3.0" apply false
}
