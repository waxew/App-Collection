# استاندارد دائمی کامنت‌گذاری سورس

این سند یک قانون دائمی برای `App-Collection` و ادامه توسعه آن است. ایجاد یا ویرایش سورس بدون به‌روزرسانی توضیحات مرتبط، تغییر کامل محسوب نمی‌شود.

## اصل اصلی

هر فایل متنی سورس که ایجاد یا ویرایش می‌شود باید تا حد عملی به‌گونه‌ای توضیح داده شود که توسعه‌دهنده‌ای که برای اولین بار فایل را باز می‌کند بتواند بفهمد فایل چرا وجود دارد، اجزای مهم آن چه کاری انجام می‌دهند و منطق غیرواضح چرا به آن شکل نوشته شده است.

## فایل‌های مشمول

- Kotlin و Java
- XML و AndroidManifest
- Gradle / Kotlin DSL
- GitHub Actions و YAML
- Shell script و سایر فایل‌های متنی توسعه
- فایل‌های پیکربندی که قالب آن‌ها از Comment پشتیبانی می‌کند

فایل‌های باینری مانند APK، JAR، تصویر و خروجی خودکار Build داخل خودشان قابل Comment نیستند. هدف و نحوه تولید آن‌ها باید در README یا مستندات همان بخش ثبت شود.

## سطح توضیح مورد انتظار

1. ابتدای فایل: هدف و نقش فایل.
2. کلاس/Object/Activity/Service: مسئولیت و ارتباط آن با بقیه پروژه.
3. تابع: ورودی، خروجی و اثر مهم آن.
4. متغیر مهم یا State: دلیل وجود و محل استفاده.
5. منطق غیرواضح: دلیل انتخاب روش، محدودیت API یا رفتار مورد انتظار.
6. Permission، Manifest، Build و Signing: دلیل نیاز و پیامد امنیتی/اجرایی.
7. Bug fix: علت خطا و دلیل اینکه تغییر جدید از تکرار آن جلوگیری می‌کند.

کامنت نباید صرفاً همان کد را با کلمات دیگر تکرار کند. برای مثال `counter++ // counter را یکی زیاد می‌کند` ارزش آموزشی کمی دارد؛ اما توضیح اینکه چرا شمارنده باید قبل از ثبت Artifact افزایش یابد مفید است.

## قانون تغییرات آینده

هر بار رفتار کد تغییر می‌کند، Comment و Documentation وابسته نیز باید همان Commit به‌روزرسانی شوند. Comment قدیمی و نادرست از نبود Comment خطرناک‌تر است.

## کد تولیدشده

فایل‌های Build/generated نباید فقط برای اضافه کردن Comment به‌صورت دستی دستکاری شوند. در صورت نیاز، Generator یا مستندات منبع باید توضیح کافی داشته باشد.

## کنترل قبل از Merge/Release

- فایل جدید بدون توضیح نقش باقی نمانده باشد.
- تابع یا بخش پیچیده بدون توضیح علت وجود نداشته باشد.
- TODO/FIXME رهاشده برای قابلیت ضروری Release وجود نداشته باشد.
- Commentهای مرتبط با API، Version، Permission و رفتار Navigation با کد فعلی هماهنگ باشند.
